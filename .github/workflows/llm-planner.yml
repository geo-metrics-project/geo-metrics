name: Build and Push LLM Planner

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - 'llm-planner/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'llm-planner/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push LLM Planner Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./llm-planner
          push: true
          tags: ghcr.io/${{ github.repository }}/llm-planner:latest
